import { ReactNode } from "react";
import "../styles/AlertCard.scss";
import { Status } from "../types/Status";

type AlertCardProps = {
    alertType: string;
}

function AlertCard({ alertType }: AlertCardProps) {
    return (
        <div>

        </div>
    );
}

export default AlertCard;